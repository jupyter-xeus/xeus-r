version: 2

build:
  os: "ubuntu-latest"
  tools:
    python: "mambaforge-26.1"

sphinx:
  # Path to Sphinx configuration file
  configuration: docs/source/conf.py

conda:
  environment: docs/environment.yml
